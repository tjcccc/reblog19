const links = {
  default: '/',
  index: '/index',
  home: '/home',
  initialize: '/initialize',
  login: '/login',
  setting: '/setting',
  post: '/post/:id',
  editor: '/editor'
}

export default links;
